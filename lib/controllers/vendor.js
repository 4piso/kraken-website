'use strict';

// global constants =================
const Wreck = require('wreck');

// intertanls ======================
const internals = {};

// get all vendor =================
internals.getProductVendor = (request, reply) => {

    // global properties
    const apiUrl = 'http://localhost:8000' + '/vendor/';

};

// exposing the modules ===========
module.exports = internals;
